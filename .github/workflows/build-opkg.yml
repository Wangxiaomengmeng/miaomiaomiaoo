name: Build Static Opkg

on:
  workflow_dispatch:

jobs:
  build-opkg:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    steps:
      - name: Install deps
        run: |
          apk update
          apk add build-base cmake git libarchive-dev openssl-dev \
                  zlib-dev json-c-dev

      - name: Clone libubox
        run: |
          git clone https://git.openwrt.org/project/libubox.git

      - name: Build libubox static
        run: |
          cd libubox
          mkdir build && cd build
          cmake .. \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DBUILD_LUA=OFF \
            -DBUILD_EXAMPLES=OFF
          make -j$(nproc)
          make install
          rm -f /usr/lib/libubox.so*
          ls -l /usr/lib/libubox.a

      - name: Clone opkg
        run: |
          git clone https://git.openwrt.org/project/opkg-lede.git

      - name: Build static opkg
        run: |
          cd opkg-lede
          mkdir build && cd build
          cmake .. \
            -DBUILD_SHARED_LIBS=OFF \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_EXE_LINKER_FLAGS="-static" \
            -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
            -DCMAKE_PREFIX_PATH=/usr \
            -Dubox_LIBRARY=/usr/lib/libubox.a \
            -Dubox_INCLUDE_DIR=/usr/include
          make opkg-cl -j$(nproc)

      - name: Copy binary
        run: |
          cd opkg-lede
          ls -l build/src/
          cp build/src/opkg-cl ./opkg
          chmod +x opkg
          ls -lh opkg
          file opkg || true

      - name: Upload opkg
        uses: actions/upload-artifact@v4
        with:
          name: opkg-static
          path: opkg-lede/opkg